export interface IBaseHeroProps {
  backgroundImageUrl: string;
  text: IBaseHeroTxt;
  btnPath: string;
}

export interface IBaseHeroTxt {
  heading: string;
  paragraph: string;
  button: string;
}
